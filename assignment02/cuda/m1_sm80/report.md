prop1.1

沿K方向相邻
这种分布使得prob1.4的ldmatrix可以使用b16，一次读取相邻的两个e4m3作为一个16位浮点数，让原本的以 8*16 tile为单位排布的A和B矩阵得以缩成 8*8 ，使得tile可以正常分配


prob1.2

我也忘了是怎么错的了，总之是A映射出问题，导致A下半个矩阵的映射全部换成了上半个矩阵，相当于最后两个上半拼成了一个A，导致所有的a2、a3、a6、a7全部替换为了对应的a0、a1、a4、a5 

prob1.4
省去了手写以线程为单位搬运数据的映射工作，可以算好每一小行的地址后以tile为单位搬运数据
不可避免是因为从内存到逻辑矩阵再到寄存器的映射过程需要手动来模拟，而ldmatrix有一部分的内置规则，可以帮我们省去一定的步骤

prob1.5
8/2 16384 8192 9.75
16/4 32768 24576 10.75
32/8 65536 57344 16.06
4/1 8192 0 9.23

64B，8warp pipeline导致实际cycle比例和wavefront不一样