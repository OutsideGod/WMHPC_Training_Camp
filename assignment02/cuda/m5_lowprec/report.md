prob 5.1

x约等于0.5/0.1/0.01/0.005/3000
相对误差为4.611%/4.634%/30.85%/100%/0

1. 去掉outlier后，0.5附近误差从4.611%降到0.03086%，减小149.4倍，因为原来的scale被3000决定

2. scale=3000/448=6.696429，输入归零阈值为scale/1024=0.006539481

3. 不含outlier的1 * 128 block误差为0.1104%，含outlier的block为4.775%，per-block不能消除outlier，但可以把影响限制在一个block内


prob 5.2

1. row/column scale中scale乘积与k无关，可以在完整点积后乘回
K-block scale中每段scale不同，因此每段partial sum必须先乘回对应scale再累加

2. GEMM本来就沿K方向归约，让一个scale负责连续16或32个K元素，可以和同一段fragment一起供给Tensor Core，不会把不同scale的partial sum混在一起

3. 粒度16把outlier影响范围从128个元素缩小到16个，但每128个元素的scale从1个增加到8个，metadata和供数复杂度都是原来的8倍

两个正确函数pytest全部PASS


prob 5.3

1. E2M1共有0、0.5、1、1.5、2、3、4、6八个幅值，中点使用round-to-nearest-even，大于6饱和
B300上与硬件转换逐位对拍202864个值，全部PASS

2. 每个线程处理K方向连续16个元素，计算amax和E4M3 scale，再用硬件F2FP.E2M1打包
量化结果逐byte判测PASS，4096 * 7168性能为34.78us、2163GB/s
cuBLASLt消费结果的maxrel约为3.9e-3，说明data和scale布局正确

3. 相同形状的纯访存probe为10.32us、7287GB/s，quant只达到它的29.7%
NCU中quant的DRAM和SM利用率为11.42%和21.71%，差距主要来自amax、scale、除法和格式转换的指令依赖，不是显存带宽打满


prob 5.4

顺序为M/K、两步us、融合us、加速比

1/4096 8.21 4.62 1.78
16/4096 8.21 4.67 1.76
256/4096 8.20 6.16 1.33
1024/4096 14.34 12.30 1.17
4096/4096 32.59 36.23 0.90
16384/4096 113.31 135.68 0.84
4096/7168 57.58 59.49 0.97
16384/7168 203.83 233.94 0.87
4096/8192 63.62 67.67 0.94
16384/8192 223.03 262.53 0.85

十组正确性全部PASS

1. M较小时少一次kernel launch和bf16中间值，因此有1.17到1.78倍加速

2. M>=4096后，两步实现可以让RMS和quant分别使用更合适的grid，融合以后一个CTA同时负责归约和量化，生命周期和同步开销更大，因此反而慢3%到16%

3. 融合实现只达到纯访存probe的17.3%到68.8%，2.56倍只是按字节量计算的上限，实际还要完成RMS归约、乘weight、amax、scale和E2M1转换


prob 5.5

1. W4A16加Marlin属于存储量化，权重用int4保存，计算时仍使用fp16
NVFP4属于计算量化，量化值和block scale直接进入FP4 Tensor Core

2. Marlin节省显存容量和带宽，不提高fp16计算吞吐
NVFP4同时节省容量、带宽并提高计算吞吐，但增加了scale和量化开销

3. 小batch decode主要卡权重读取，因此存储量化的收益更直接，NVFP4额外的计算吞吐要到M较大时才更有用
